# Habit Tracker API (Python)

FastAPI sample with hello route and in-memory habits CRUD.

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app:app --reload --port 5000
```
